# HANDOFF — 🎛 ORCHESTRATOR 17 → next seat (2026-08-29)

**State: XYZ-1742 + XYZ-1827 v0.1 are BUILT, WOVEN, DEPLOYED, and RUNNING** behind the
operator's live deck (`localhost:3131`). This file is the seat-transition doc; the deep
record is `docs/goals/xyz-1742-fleet-lifecycle/` (packs, reports, contract, audit).
The old pre-build pack (`HANDOFF-XYZ-1742-orgchart.md`) carries a SUPERSEDED banner — never act on it.

## What runs (verified this morning)

- **Lifecycle backend** in `server.js`: leases/epochs/heartbeat, seats + fencing, CAS
  two-phase reaper (76/76 on the box). Live: seat `orchestrator` epoch 2 (mine — it goes
  suspect when my session dies; claim fresh, the epoch bump fencing me out is by design).
- **Hooks**: box + mac installed (`~/.claude/fleet/`). Mac needs `FD_NAME` per session
  (`fleet.env` sets `orchestrator`); the four FD workers carry `parent: mac/orchestrator`.
- **Coordinator v0.1** — CANONICAL HERE (operator ruling 2026-08-28; lowcap copy
  superseded, its adjudication history ported at `9430650`). `coordinator/` + the
  `coordinator-run` / `coordinator-portal` skills. Boot gate ships DISARMED.
- **Org chart tab** — current render is grouped cards; the node-link tree rework is IN FLIGHT.

## In flight: Dagmar (frontend-developer, Claude breed, fast ON)

`FD-dagmar` on the box, branch `agent-dagmar`, goal: node-link tree per the operator's
reference (parent-child only, NO host grouping — operator ruling, see
`docs/goals/xyz-1742-fleet-lifecycle/lane-dagmar-tree.md` if present, else her launch
prompt `launch/launch-dagmar.txt`). On her finish: review screenshots, merge
`agent-dagmar` → train branch → `main`, push (broker route), reload the deck —
`public/`-only changes need NO deck restart; `server.js` changes need the operator's
`./up.sh` (agent-started decks cannot sign certs — never start the deck yourself).

## ~~Pending~~ RESOLVED: the six rulings came back and were EXECUTED by O17 (2026-08-29)

Verbatim record + consequences: `docs/goals/coordinator-v02/OPERATOR-RULINGS-2026-08-29.md`.
Applied: cleanup done (FD workers killed, names released); **Alistair** (platform-engineer,
Claude, fast ON) is IN FLIGHT on Coordinator v0.2 = proposal M1-M5 + M6 initiation ritual
(northstar/lanes/lane_cap collected from the operator via grill-me at init; hard cap 6
ABOLISHED by ruling R3; one coordinator per project) + the box stop-hook rider. Board
seeds stay PROPOSED until the ritual runs (R1/R2). Boot gate arms at v0.2 weave (R4).
On Alistair's finish: weave agent-alistair, then ARM the gate (stanza in his report).
The sheet (historical): https://claude.ai/code/artifact/0a747563-ecef-4a36-9d3e-8cfc25155f9e
The guidance below is superseded by the rulings file but kept for mechanics:
- `northstar` / `seed-lanes` / `free-slots` → a **coordinator run** (drop a sitrep or edit
  via the run skill; never hand-edit board.json outside a run; `check.py` must stay green).
- `boot-gate: arm-now` → add the stanza from `coordinator/hooks/README.md` to the Mac's
  `~/.claude/settings.json` (backup exists from Konrad's installer pattern).
- `v02-routing` → package from the researcher's proposal (lowcap branch, doc
  `docs/research/coordinator-v02-scope-proposal.md` @ `1e3e9470` — PORT it in, provenance
  note like `docs/coordinator/IMPORTED.md`). M5 compaction is a dependency of M1/M3:
  bundle is at 87% of the 8KB gate with 4 lanes.
- `fleet-cleanup` → kill FD-{edith,konrad,alfons,sylvia} tmux sessions, release the four
  names (`name.py close <Name>`), fix the box stop-hook wart (Konrad's SessionEnd hook
  emits JSON Claude rejects — small fix lane), mark registry rows.

## Doctrine (short form — full: `/how-to-fleet-git`, german-box-workers skill)

Credentials are fleetdeck-only: broker askpass for GitHub, `gb-deploy` cert for ssh,
NEVER 1Password. You launch lanes yourself and push yourself; escalate only for: box
dead, key window shut (train/cert = Touch ID), or the operator claiming a step.
Fast-mode panel over tmux: `/fast` + Enter opens, **Space toggles**, Escape closes.
Registry every launch (`group`, ONE task key). Bus (`bin/fleet-message.js`) for worker
messages — never type into panes; unsubmitted-input quirk needs a second Enter.

## Corrections ledger (so no ghost re-litigates)

- Lowcap `agent-sylvia` = superseded; deletion pending on XYZ-1827 (researcher posted the
  note; their session closed — confirm it happened before assuming).
- XYZ-1742 lane sub-issues: XYZ-1818 (Alfons), 1819 (Konrad), 1820 (Edith), 1823 (Edith's
  push gate); coordinator = XYZ-1827. Linear MCP is NOT connected in desktop seats —
  workers post, or packs-branch fallback.

## Boot ritual for you

1. `sh ~/.claude/session-kind/mark.sh --orchestrator "<topic>"` (fresh number).
2. Read this file; `git log --oneline -15`.
3. `ssh gb-deploy "wsl tmux capture-pane -p -t FD-dagmar"` — is she done, working, or stalled?
4. Probe: deck `/api/health` (reaper tick fresh?), broker `/api/ghtoken`.
5. Claim the orchestrator seat (`POST /api/seats/claim`, owner mac/orchestrator).

— 🎛 ORCHESTRATOR 17, closing clean.
