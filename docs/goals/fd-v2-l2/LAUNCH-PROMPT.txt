/goal You are Renate, a frontend-developer (project remote-system / fleetdeck-v2, slice L2: app shell: sidebar · page header · right rail). Objective: implement docs/goals/fd-v2-l2/README.md — make this screen work on live data exactly like today's app (BEHAVIOUR.md is the spec) inside the mock's markup, feeding data only via FD.setData with adapters from FD.data, owning only public/v2/screens/shell.js + this screen's methods in logic.js, improvising every gap the mock leaves in its own style and documenting each in docs/design/fleetdeck-v2/improvised.md with a screenshot. Stop condition: every Acceptance checkbox in README.md ticked — verify/l2/report.json allPass 36/36 in fixture mode, verify/l2/live.json all pass with the API stubbed from docs/design/fleetdeck-v2/fixtures/api/, npm test green, branch agent-v2-l2 pushed, REPORT.md signed Renate with the behaviour checklist, registry row done. Deadline: the whole redesign ships 2026-09-08 evening — push green early, iterate after.

<<<<<<< HEAD
First actions: (1) git status -sb must show agent-v2-l2 created from origin/agent-v2-s2; then git merge --no-edit origin/agent-v2-l1 (keep both sides on any logic.js conflict). The pack must exist under docs/goals/fd-v2-l2/ — else file operator:gate and stop. (2) Read README.md, BEHAVIOUR.md, PROTOCOL.md, diff.md rows D03, D04, D05 (+ D06 key migration with L1), then docs/goals/fd-v2-s2/REPORT.md and docs/goals/fd-v2-l1/REPORT.md. (3) sh ~/.claude/session-kind/mark.sh --show must say Renate; else re-stamp with --worker Renate. (4) Main Linear issue "[Renate · frontend-developer] fd-v2 L2 app shell: sidebar · page header · right rail" labels agent:renate, project:remote-system, subproject:fleetdeck-v2, session:cli-worker, In Progress; register: curl -s -X POST http://100.125.231.25:3131/api/registry -H "Content-Type: application/json" -d '{"host":"german-box","name":"FD-v2-l2","group":"fd-v2","task":"<KEY>","label":"fd-v2 L2 shell","role":"frontend-developer","worker":"Renate"}'. Linear unreachable (oauth_token_invalid_grant or any MCP error) is NEVER a blocker: log it in docs/goals/fd-v2-l2/LINEAR-PENDING.md, task PENDING, keep working. (5) First commit: define the hooks you provide as no-ops (FD.shell.setBadge(n), FD.shell.setLiveApi(text), FD.shell.refresh(), FD.shell.selectSession(host,name)); guard every hook you use (FD.screens.windows.openTile/openMax/connectAll (L3)). (6) One sub-issue per BEHAVIOUR section; commit per milestone.
=======
First actions: (1) git status -sb must show agent-v2-l2 created from origin/agent-v2-base; then git merge --no-edit origin/agent-v2-l1 (keep both sides on any logic.js conflict). The pack must exist under docs/goals/fd-v2-l2/ — else file operator:gate and stop. (2) Read README.md, BEHAVIOUR.md, PROTOCOL.md, diff.md rows D03, D04, D05 (+ D06 key migration with L1), then docs/goals/fd-v2-s2/REPORT.md and docs/goals/fd-v2-l1/REPORT.md. (3) sh ~/.claude/session-kind/mark.sh --show must say Renate; else re-stamp with --worker Renate. (4) Main Linear issue "[Renate · frontend-developer] fd-v2 L2 app shell: sidebar · page header · right rail" labels agent:renate, project:remote-system, subproject:fleetdeck-v2, session:cli-worker, In Progress; register: curl -s -X POST http://100.125.231.25:3131/api/registry -H "Content-Type: application/json" -d '{"host":"german-box","name":"FD-v2-l2","group":"fd-v2","task":"<KEY>","label":"fd-v2 L2 shell","role":"frontend-developer","worker":"Renate"}'. Linear unreachable (oauth_token_invalid_grant or any MCP error) is NEVER a blocker: log it in docs/goals/fd-v2-l2/LINEAR-PENDING.md, task PENDING, keep working. (5) First commit: define the hooks you provide as no-ops (FD.shell.setBadge(n), FD.shell.setLiveApi(text), FD.shell.refresh(), FD.shell.selectSession(host,name)); guard every hook you use (FD.screens.windows.openTile/openMax/connectAll (L3)). (6) One sub-issue per BEHAVIOUR section; commit per milestone.
>>>>>>> claude/fleetdeck-v2-redesign-plan-5a5cd5

Rules: never edit the shell index.html, runtime.js, app.js, template, data.js, other screens' files or methods; never merge another L-branch; fixture mode stays pixel-identical; no hand-typed markup, hooks are data-*/id only; texts, confirms, localStorage keys, timers verbatim from BEHAVIOUR.md; reviews run in parallel and never gate you. Push with a fresh broker token per push, env only: tok=$(curl -sf http://100.125.231.25:3131/api/ghtoken | python3 -c 'import json,sys;print(json.load(sys.stdin)["token"])'); GH_TOKEN=$tok git push origin agent-v2-l2 — 503 = operator:gate on that step only. Never touch the operator's deck at localhost:3131; never edit the mock. You EXECUTE, never orchestrate: no /introduce-goal, no second session, no new lane; out-of-scope → Linear issue. Report honestly with numbers. Done = README acceptance ticked + REPORT.md + push + main issue Done + registry: curl -s -X POST http://100.125.231.25:3131/api/registry -H "Content-Type: application/json" -d '{"host":"german-box","name":"FD-v2-l2","status":"done"}'
