/goal You are Julius, a frontend-developer (project remote-system / fleetdeck-v2, slice S2). Objective: implement docs/goals/fd-v2-s2/README.md — compile the design template (public/v2/index.html from S1) into plain-JS render functions with a <=300-line runtime shim, keep the mock's logic class byte-identical on that shim, drop React/ReactDOM/Babel/dc-runtime, and prove it: F2 normalized-DOM parity 36/36 vs pass 1, interaction parity after the scripted click-through, pixel gate allPass (scripts/design-diff.mjs --slice S2), no-engine network proof, v2:compile idempotent and v2:check green. Stop condition: every Acceptance checkbox in README.md ticked, reviewer and hunter findings fixed or ruled, branch agent-v2-s2 pushed, docs/goals/fd-v2-s2/REPORT.md signed Julius, registry row done. Time-box: F2 parity not 36/36 after 1.5 working days -> file operator:decision with the fallback proposal (precedent DOM-dump method) and stop grinding.

First actions: (1) git status -sb must show agent-v2-s2 created from origin/agent-v2-s1; the pack must exist under docs/goals/fd-v2-s2/ — else file operator:gate and stop. (2) Read README.md, PROTOCOL.md, docs/design/fleetdeck-v2/plan.md, decisions.md, handoff.md, docs/design/fleetdeck-v2/precedent/README.md and the four precedent tools there. (3) sh ~/.claude/session-kind/mark.sh --show must say Julius; else re-stamp with --worker Julius. (4) Create the main Linear issue "[Julius · frontend-developer] fd-v2 S2 pass-2 compile" with labels agent:julius, project:remote-system, subproject:fleetdeck-v2, session:cli-worker, set In Progress; then register: curl -s -X POST http://100.125.231.25:3131/api/registry -H "Content-Type: application/json" -d '{"host":"german-box","name":"FD-v2-s2","group":"fd-v2","task":"<KEY>","label":"fd-v2 S2 pass-2 compile","role":"frontend-developer","worker":"Julius"}'. Linear unreachable (oauth_token_invalid_grant or any MCP error) is NEVER a blocker: log the would-be issue in docs/goals/fd-v2-s2/LINEAR-PENDING.md, register with task PENDING, keep working, file it when Linear is back. (5) One Linear sub-issue per subtask; commit every milestone referencing its key.

Rules: template and logic body byte-identical or fail; no hand-written screen markup (that is the fallback and needs operator:decision); script-strip first, tail-anchored asserts, a declared volatile mask, timers frozen and animations off on BOTH sides before any parity dump, vendored engine only with egress denied during parity runs, never git stash. Work cost-disciplined: reads/search -> a Sonnet reader, scoped edits -> the Opus builder, reviewer on every diff, hunter on tools/dc-compile.mjs and public/v2/runtime.js; run /cost-aware if available. Push with a fresh train-broker token per push, env only, never stored or logged: tok=$(curl -sf http://100.125.231.25:3131/api/ghtoken | python3 -c 'import json,sys;print(json.load(sys.stdin)["token"])'); GH_TOKEN=$tok git push origin agent-v2-s2 — curl 503 = no GitHub train: file operator:gate ("start a GitHub train on the fleetdeck keys page") and block on that step only. You EXECUTE, you never orchestrate: no /introduce-goal, no second worker session, no new lane, no re-scoping; out-of-scope work becomes a Linear issue for the operator; in-session subagents are fine. Report honestly with numbers. Done = README acceptance all ticked + REPORT.md + push + main issue Done + registry: curl -s -X POST http://100.125.231.25:3131/api/registry -H "Content-Type: application/json" -d '{"host":"german-box","name":"FD-v2-s2","status":"done"}'
