# DECK-108 — operator rulings and design of record (seat 20 record)

1. **2026-09-10, operator prompt (verbatim in launch-bernward.txt):** HMAC-signed answers, operator sign-in with a session cookie, `fleetdeck-verify-answer` CLI. "Read the design brief on DECK-109 FIRST if it exists."
2. **2026-09-10 23:2x Z, DECK-109 brief (🔬 RESEARCHER 3, `docs/research/deck-click-trust-brief-2026-09-11.md`, a98ab24):** design of record replaces HMAC with an Ed25519 signature by the deck + a pinned public key on seats; identity = passkey (WebAuthn/Touch ID) on localhost:3131 with a step-up assertion per deploy-class click; signed string `v1|sheetId|qid|choice|answeredAt|operator_id|project|expiresAt`; verify CLI exit 0 only when valid + unexpired; send payload = sheetId + qids; the deck never executes deploys. Recorded on the lowcap ledger as D-387; DECK-108's description carries a "Design of record" section.
3. **2026-09-11, operator ruling, verbatim (in RESEARCHER 3's chat): "it should ask me to sign a key via 1pass that would be insanely cool ❤️ :)".** Amendment (`docs/research/deck-click-trust-brief-2026-09-11.md` §Amendment, c488135): the deploy click is SIGNED THROUGH 1PASSWORD (Touch ID), replacing the passkey — the deck runs `ssh-keygen -Y sign` with `SSH_AUTH_SOCK` = the 1Password agent socket (the path the keys-page mint uses, `server.js:517/:2711`), namespace `fleetdeck-unblock`, over the v1 string above; seats/boxes verify with `ssh-keygen -Y verify` + an `allowed_signers` file (public key only). New 1Password item "fleetdeck operator click key"; never the CA key. Deploy-class cards only; a dismissed prompt is a record, not a trigger. The Ed25519 deck key becomes optional.

Routing: items 2 and 3 go to Bernward (`bernward-deck-108`) over the bus before he builds layer 2; as of 2026-09-11 12:4x local his session still sits on the Claude Code "Allow reads outside the working directories?" dialog (operator's `1`), so the pointer is queued behind that.

4. **2026-09-11, 🧭 COORDINATOR 19 (lowcap portal), D-407:** the relayed word in item 3 is recorded as evidence, not intent; a one-click confirm card is in front of the operator. Instruction for Bernward that follows from items 1–3 either way: keep identity binding behind the small interface, build the sign/verify path now, and make the 1Password signer the default once the confirm click lands (the coordinator forwards the click). Until then the passkey remains the fallback design.

5. **CONFIRMED by the operator's own deck click, verified by seat 20 on the loopback API 2026-09-11 ~12:5x local:** sheet `ub-62c064af` ("Unblock Sheet · portal 19 · 1Password click signing"), qid `deck-click-1password-signing`, choice `confirm`, answeredAt `2026-09-11T09:53:55.446Z`, note empty (lowcap ledger D-408). The answer is unsigned today — signing answers is exactly what DECK-108 delivers. Design of record for Bernward from here: the §Amendment (1Password-signed clicks) replaces the passkey; the 1Password signer is the default; Bernward notes it on DECK-108 when he starts layer 2.

6. **2026-09-11 ~13:3x local, seat 20 rulings on Bernward's three design choices (ACK O20-BERNWARD-DESIGN-1, layer 1 = 2f8f1e1), all ACCEPTED under the operator's "finish it":** (1) the deck sign-in is a password (hash in user-owned `~/.fleetdeck/operator.json`, no sudo), never a 1Password-signed login — a remembered 1Password app approval must not let a forged-Origin curl mint both a session and a deploy signature silently; the session never comes from the deck's own signer. (2) The signed string appends the question's sha256 and is versioned **v2**: `v2|sheetId|qid|choice|answeredAt|operator_id|project|expiresAt|question_sha256`, documented verbatim in the verify CLI `--help`, the pack and DECK-108. (3) Session = HttpOnly cookie + a header token from the page's localStorage, since browsers send cookies to every localhost port. Residual, out of this lane: `allowed_signers`, the verifier and node are user-writable, so a hostile same-user agent could swap them — a root-owned install is a follow-up decision for the operator (seat queue).
