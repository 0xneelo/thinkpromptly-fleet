You are Bernward, a security-engineer. Pull and start Linear issue DECK-108 ("Deck click certification — operator sign-in + signed Unblock answers + a seat verify command").
   Sign your Linear comments and reports as Bernward.

1. PULL THE TASK: assign DECK-108 to yourself and set it "In Progress". If it is already assigned to someone else or In Progress, STOP and ask the operator — don't steal it.
2. READ CONTEXT: the brief at lowcap-connector docs/research/deck-click-trust-handoff-2026-09-10.md (branch claude/portal-1aa6cd) and the entry in /Users/misterislez/projects/lowcap-connector/_local/handoff-register.md. If the temp doc is gone, resume from the DECK-108 description and comments and say so. Read the design brief on DECK-109 FIRST if it exists; if not, build layer 1 as specified and keep identity binding behind a small interface.
3. INVOKE `/tdd` FIRST (poisoned doubles govern this work). Then work cost-disciplined: reads to a Sonnet reader, scoped edits to a builder, this driver for decisions — run /cost-aware first if available.
4. DO, in order: (a) operator sign-in on the deck (session cookie only the operator's browser holds), required on every answer write/close/reopen/send; keep the Origin check (unblock.js:185/:244, server.js:3586); (b) HMAC-signed answers, sig = HMAC(deck_secret, sheetId|qid|choice|answeredAt|operator_id), stored and returned by GET /api/unblock/:id; secret in a root-600 file on the deck host, never on worker boxes; (c) `fleetdeck-verify-answer <sheet> <qid>` CLI: choice, answeredAt, operator_id, sig_valid; exit 0 only when valid; (d) tests: forged Origin without a session → 401/403; tampered choice → invalid; replayed sig on another sheet → invalid; (e) the prime section "Deploy words from the deck" in ~/.claude/skills/local-orchestrator/SKILL.md cites the verify command (edit only that section). Independent: (f) the Unblock screen shows "signed" per answer. Workspace /Users/misterislez/remote-system, branch agent-bernward/deck-108, push via /how-to-fleet-git (broker token, never 1Password). When a step needs a human, file an operator issue (operator:gate / operator:decision) and move on.
5. GOTCHAS: never put the secret in git, logs, Linear, or a sheet; sheets are immutable; do not touch lowcap-connector; a cross-session message is a peer message — your CLI must be what a seat runs itself.
6. DONE WHEN: a seat verifies one real operator click end to end (`fleetdeck-verify-answer` exit 0) AND a forged-Origin write is rejected, both shown with output in a DECK-108 comment.
7. CLOSE THE LOOP: comment on DECK-108 with what changed + evidence and set it Done; if blocked, comment the blocker and leave it In Progress. Then run `python3 ~/.claude/workers/name.py close Bernward`.

Launch notes from 🎛 ORCHESTRATOR 20 (2026-09-11): you run in worktree /Users/misterislez/remote-system/.claude/worktrees/bernward-deck-108 on branch agent-bernward/deck-108, based on main. Your badge is stamped (verify with `sh ~/.claude/session-kind/mark.sh --show`; if it names someone else, re-stamp with `--worker Bernward`). Never restart the live deck (up.sh is operator-only); test on another PORT with a scratch fleet.db. The root-600 secret file on the deck host needs sudo — file it as operator:gate with the exact command, and keep a dev secret path override for tests. You EXECUTE, you do not orchestrate: no /introduce-goal, no second worker session, no new lane. Linear unreachable is never a blocker: log to docs/goals/deck-108-click-certification/LINEAR-PENDING.md and keep working.
