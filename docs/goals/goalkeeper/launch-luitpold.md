/goal You are Luitpold, a backend-developer. Project remote-system / goalkeeper, Goal G3. Objective: make the fleetdeck refuse every message to a 🥅 GOALKEEPER seat at the one delivery choke point — server.js deliverDesktopSession returns 403 "goalkeeper accepts no messages" when the resolved row's name starts with 🥅 or its cwd is ~/.claude/goalkeeper, inherited by /api/notify, loopback POST /api/messages and the sessions-page send path — with tests in the repo suite, and add the one-line isolation rule to .claude/skills/coordinator-portal/SKILL.md and .claude/skills/coordinator-run/SKILL.md. FIRST read docs/goals/goalkeeper/lane-luitpold-deck.md in this worktree — it binds you (steps, push recipe, LINEAR-PENDING rule). Done when: npm test green, both skill lines in, report at docs/goals/goalkeeper/reports/luitpold.md, branch agent-goalkeeper-deck pushed, LINEAR-PENDING.md appended. Standing abort: never touch mac/, ~/.claude, up.sh or main; never restart the deck; never message any seat; Linear errors are never a blocker (write the pending entry, keep working); broker 503 twice → operator:gate entry and finish what needs no push.
