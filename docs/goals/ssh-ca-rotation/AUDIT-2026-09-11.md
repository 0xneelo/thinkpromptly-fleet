# Audit of agent-ssh-ca-rotation @ bce9045 — findings for Ivo to fix before the weave

Commissioned by 🎛 ORCHESTRATOR 20 on 2026-09-11 ~00:25Z. Static review only; no host, no ssh, no minting. Part A is the hunter (gpt-6-astra xhigh, session hunter-20260911-002431-60768). Part B (oracle verdict) is appended when it lands. Fix every High and Medium, add a test or a dry-run proof per fix, push, then reply ACK with the SHA. Lows are optional.

## A — hunter findings (severity-ranked)

1. **High — `deploy-keys/lib/host_apply.py:154`, `deploy-keys/apply-principals-windows.ps1:21`.** An earlier `Match` block (e.g. `Match Address 10.0.0.0/8` + `PasswordAuthentication yes`) beats the appended global `PasswordAuthentication no` (first matching declaration wins). Linux validates only against `127.0.0.1`; Windows has no effective-policy check. Fix: validate with `sshd -T -C user=deploy,addr=<each non-loopback source>` (or insert the prohibition inside a `Match User deploy` block placed BEFORE any existing Match), and on Windows evaluate the effective config the same way.
2. **High — `host_apply.py:238`, `deploy-keys/lib/windows-apply.ps1:139`.** Trust and principals applies read `sshd_config` without a lock; a principals run can install a snapshot that lacks the `TrustedUserCAKeys` the trust run just wrote, and its validation still passes. Fix: one lock file per host apply (`flock` on Linux, a mutex on Windows), re-read the config after acquiring it, and assert the other run's directive is present before writing.
3. **High — `deploy-keys/verify-cert.sh:46`.** `IdentityFile`/`CertificateFile` accumulate with the alias's configured ones; `IdentitiesOnly=yes` does not exclude configured identities, so a v1 cert already on the alias can authenticate and the script prints PASS for a v2 cert that is untrusted. Fix: run with `-F /dev/null` plus explicit `-o HostName/User/Port` from `ssh -G <alias>`, or `-o IdentityFile=none -o CertificateFile=none` before the explicit pair.
4. **High — `verify-cert.sh:58`.** `--expect refused` passes on any `Permission denied` — a `ProxyJump` rejecting its credentials, or an empty credential file. Fix: require the target host key exchange to complete (`-v` marker `Authenticating to <host>:<port>`) and distinguish the refusal of the presented cert (`Server refused certificate` / `no mutual signature`) from other failures; preflight that the cert file parses with `ssh-keygen -L`.
5. **Medium — `windows-apply.ps1:143`.** Policy files are truncated and rewritten in place while sshd is live. Fix: write to a temp file in the same directory, then `Move-Item -Force` (atomic on NTFS).
6. **Medium — `host_apply.py:77`.** Under `umask 077`, `mkdir(mode=0755)` yields `0700` for `/etc/ssh/principals`; sshd reads principals files as the authenticating user and cannot traverse. Fix: `os.chmod` after mkdir (and on the files), assert the final modes.
7. **Medium — `host_apply.py:216`, `windows-apply.ps1:113`.** `--rollback` validates the CURRENT (possibly malformed) config before restoring the backup, so a broken config can never be rolled back. Fix: validate the backup, restore it, then validate.
8. **Medium — `host_apply.py:248`.** In the failure handler, a failing `usermod` (account DB locked) raises inside the handler and skips every restore below it. Fix: wrap each rollback step, collect errors, restore files first, disable the account last.
9. **Medium — `deploy-keys/mint-deploy-cert.sh:40` vs `:71`.** The CA fingerprint is classified from the pub file, then the file is re-read at signing time; a change in between yields a v1-signed cert tagged `-ca2` (or the reverse). Fix: read the pub once into a variable and sign from that exact content (temp file with `umask 077`).
10. **Medium — `verify-cert.sh:64`.** Windows output is `IVO_CERT_OK deploy\r\n`; `grep -qx` rejects the marker because CR survives. Fix: `tr -d '\r'` before comparison.
11. **Medium — `verify-cert.sh:50`.** A certificate path with spaces is reparsed by ssh's config parser (`CertificateFile=/tmp/cert set/...`). Fix: pass `-o 'CertificateFile="<path>"'` with quotes ssh understands, or refuse paths with whitespace.
12. **Low — `deploy-keys/bootstrap-rog-strix.sh:15`.** The newline guard `*'\n'*` matches a literal backslash-n (`C:\new-ca.pub`). Fix: test with `case "$p" in *"$(printf '\n')"*)`.

Not flagged by the hunter (treat as not-yet-confirmed clean, not proven): `render-sudoers.sh`, `setup-german-box-ca.ps1`, `setup-rog-strix-ca.ps1`, the `server.js` mint-route hunk.

## B — oracle verdict (Fable xhigh, 2026-09-11 ~01:30Z): SAFE WITH FIXES

Do not deploy the deck from this branch until H1 is fixed; no owner runs S2/S3 until M1–M3 are fixed. Privilege checks hold (`--daily` = exactly `-O clear -O permit-pty`, tags validated twice, `CA_PUB` not reachable from HTTP, sudoers one exact line, `useradd --password '*'`, legacy login kept in principals). No secrets. Tests: 46 focused, 40 pass, 6 correctly gated skips; mint-route test passes.

**H1 — the keys screen can no longer mint a cert any host accepts, and every UI mint repoints `deploy-certs/current`.** `keys.js:52-61`, `server.js:2685-2697` (legacy `{ttl, principals}` → 400), `mint-deploy-cert.sh:73`. One Daily click before S3 lands everywhere → `current` = a `deploy`-only cert → every alias and collector fails (this exact class happened tonight with a `tabor`-only cert). After S3 it persists: all aliases share one `current`.
**Seat 20 decision (binding for the fix):** (a) the keys screen keeps a **Legacy** mint — principals `root,vibe,misterisley,tabor`, the default until S3 is applied on all hosts (a `deploy-keys/ROTATION-STATE` file or env flips the default later); Daily and Admin stay as additional modes; the mint route accepts `{profile: legacy|daily|admin, ttl, extraTags}` and still 400s unknown fields. (b) `mint-deploy-cert.sh` writes `current-daily` / `current-admin` for the new profiles and leaves `current` to legacy; `ssh-config.roles.example` points `*-deploy` at `current-daily` and `*-admin` at `current-admin`; nothing repoints `current` except a legacy mint. (c) The keys screen refuses a legacy mint whose principals drop a login user still present in `machines.json` (rider from memory `mint-repoints-current-for-all-aliases`).

**M1** `windows-apply.ps1:134-137` — `New-LocalUser deploy` without `-PasswordNeverExpires -UserMayNotChangePassword`: cert logins die when the password ages out (42 days). Fix: add both.
**M2** `windows-apply.ps1:177-180` — scheduled restart task lacks `-Settings (New-ScheduledTaskSettingsSet -AllowStartIfOnBatteries -DontStopIfGoingOnBatteries)`; on a laptop on battery the task never runs. Runbook: the Windows restart drops every SSH session; before apply, the owner proves a non-cert admin login (`wsl-machine` key) works — name it.
**M3** `host_apply.py:50-54, 89-101` — `directive()` deletes every `TrustedUserCAKeys`/`AuthorizedPrincipalsFile` line (including inside `Match`) and validates only the planned config; a host trusting v1 via `sshd_config.d/*.conf` or a Match block is retargeted to a file holding only v2 → v1 refused right after "Applied". Fix: run `sshd -T` on the BASELINE and refuse when `trustedusercakeys != /etc/ssh/deploy_ca.pub` (trust) or `authorizedprincipalsfile != none` (principals); assert the v1 fingerprint is present in the planned trust file unless `--retire-v1`.
**M4** `AGENT.md:9-11,36-38` — describes the target state as live. Fix: a "Today (until S3)" section with the current aliases/users, the Connect form and the german-box cmd/WSL note restored.
**M5** `keys.js:54` — Admin chips always prepend `admin`, so `<box>-only` scoping is CLI-only. Fix: a third mode "Box-only admin" (tags without `admin`) or drop the chips; say which in the report.
Low: **L1** `host_apply.py:77` explicit `chmod 0o755` (same as hunter A6). **L2** `apply-principals-windows.ps1:31-35` inspect only Administrators/privileged groups (orphaned SIDs throw). **L3** `docs/design/fleetdeck-v2/verify/l7-harness/live.mjs:279,294` still posts `{ttl, principals}` → update to the new payload. **L4** derive the sshd validator path from `Win32_Service` PathName. **L5** runbook Windows rows: where the repo is staged, and `powershell -NoProfile -ExecutionPolicy Bypass -File …` (german-box default shell is cmd). **L6** `render-sudoers.sh:5` strip a trailing `.service` before appending.

Not verified by the oracle: any Windows execution, live host trust layout, shellcheck.

## C — post-fix review of 8f3dd19 (H1), 2026-09-11 ~01:55Z — NOT DONE YET

(b) and A9 match the decision: `mint-deploy-cert.sh` writes `current-daily` / `current-admin` and never touches `current` for those profiles; `ssh-config.roles.example` points `*-deploy` → `current-daily`, `*-admin` → `current-admin`; the CA bytes are read once and the same snapshot signs. Tests cover both.

Still open, blocking the weave:
- **C1 (High) — decision (a) not implemented.** `server.js` `mintArguments()` (~2685-2696) rejects `profile: legacy` (only daily|admin); `public/v2/screens/keys.js:39` `PROFILES` has no `legacy` entry. Required: `legacy` profile in the route (principals `root,vibe,misterisley,tabor`, ttl 1h|4h|8h, no extraTags) and a **Legacy** mode on the screen.
- **C2 (High) — default is not Legacy.** `keys.js:139` `var profile = 'daily'`. Required: `legacy` is the default until a `deploy-keys/ROTATION-STATE` file (or env `SSH_ROTATION_STATE=s3-applied`) says S3 is applied everywhere; then `daily` becomes the default.
- **C3 (High) — decision (c) not implemented.** No guard refuses a legacy mint whose principals drop a login user still in `machines.json`. Required: the route derives the login users from `machines.json` (add a `user` field per machine if absent, matching the ssh alias `User`) and returns 400 when a legacy mint's principals omit one; test it.
- **C4 (Medium)** `server.js:2737-2753` `deleteCertDir` only handles `current`; deleting the active daily/admin dir leaves `current-daily` / `current-admin` dangling. Required: unlink whichever of the three symlinks points at the deleted dir.
- **C5 (Low)** `machines.json:1` `_comment` still says aliases use `deploy-certs/current`; update.
- **C6 (Low)** = oracle L3: `docs/design/fleetdeck-v2/verify/l7-harness/live.mjs:279,294` still posts `{ttl, principals}`; update to the new payload (a legacy mint is `{profile:'legacy', ttl}`).
