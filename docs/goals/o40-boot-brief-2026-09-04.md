# O40 boot brief — lowcap-connector seat, 2026-09-04 (from 🧭 COORDINATOR 6)

You are the next lowcap-connector orchestrator seat (O40). O39 (🎛 ORCHESTRATOR 5) re-froze at 18:35Z, badge cleared, number 5 released. Nothing lowcap has a live seat until you boot.

## Boot, in this order
1. `/local-orchestrator` (you are already in a fresh desktop session inside `~/projects/lowcap-connector`).
2. `/orchestrator-handoff` BOOT — the handoff is `docs/goals/HANDOFF-2026-09-04-o39-to-o40.md` on `origin/main` @ `b0944a9d` (addenda A1–A4: "ship it", the deploy, the acceptance read, the Ignaz lane). Read it whole.
3. Coordinator boot-read gate (D-1): read `~/remote-system/coordinator/board.json` and `~/remote-system/coordinator/decisions-effective.md` (D-19 → D-29 were all written today), then restate lane ownership in your first reply. The board's six seat lanes (L1/L2/L3/L6/L9/N3) are registered to `o39`; file CCIR-1 `owner_changed` sitreps as `o40` for each. Worker-owned lanes (L4/L5/L78/N1/N2) you proxy-report per D-20.
4. End your first reply with: `/rename 🎛 ORCHESTRATOR <N> · lowcap-connector · O40 seat (77.1 aftercare + post-77 acceptance)`.

## State you inherit (fresh as of 18:35Z)
- **Prod = train 77 `e0ed3478`**, boot 17:58:47Z, 9 lanes + Raimund (D-28), migration 0056 applied. Health false-fail on boot is XYZ-1535, never auto-revert.
- **Acceptance DEGRADED on 56 (BSC) + 8453 (Base)**: Alchemy resets the WS log subscription every ~14 s since XYZ-2020, `transport_live=false`, frames=0, rows via the metered HTTP fallback. 146 / 42161 / 88888 healthy with Ping/Pong. Operator word "hold + aftercare" = prod stays (D-29).
- **Ignaz · rust-engineer, XYZ-2029** (`LC-ignaz-xyz-2029`, launched 18:30Z, Pursuing goal) is the aftercare lane → train 77.1 tonight if possible. Yours now: shepherd, gates, weave, deploy on the operator's word "deploy 77.1".
- **First post-deploy board shard ≥19:45Z**: read acceptance for XYZ-2017 / 2018 / 2022 / 2023 / 2010 / 2019 / 2025 / 1923 / 2011 (each lane's FINAL comment names its read). Judge quiet chains at ≥10 min, census rows only if measured after 17:58Z.
- **Coordinator run 21:33:43Z**: applies the inbox (21 sitreps as o39 + yours). Bundle gate is at 94% — keep sitreps one line.
- **Cap**: 1 of 11 lanes live (Ignaz). Queue in this order after 77.1: XYZ-2027 (Urgent: sampler starvation + x33 plan) → XYZ-2026 (D-25 board move, due Mon 09-07, with DECK-11 for the deck root) → D-26 lane (XYZ-963 + XYZ-929, request-lane separation) → D-27 lane (XYZ-1349 blue-green + Quickstart, after XYZ-1941) → sweep hygiene → riders. cg_wrong verification is LAST (D-22).
- **Operator cards pending**: Fridolin's 9 (family system, sheet live at artifact f8b04b84, the operator is answering) + Lioba's 3 (from the XYZ-2024 revert, in the handoff). When the operator pastes the JSON to the portal, the portal records rulings and hands you the lane list.
- **Waiting on the operator**: the CoinGecko `/key` monthly credit number for XYZ-1697 (you PUT the ceiling once the portal relays it).

## Fleet hazards today
- **DECK-24**: the ghtoken broker flaps 502 in 10–60 s runs. Every box push needs the retry rule (10 × 30 s). The deploy cert was re-minted ~17:48Z; check `localhost:3131/keys.html` before assuming the box is reachable.
- **DECK-23**: deck notify to a desktop seat says delivered and never renders. Talk to the portal with `SendMessage` to `🧭 COORDINATOR 6` (or `mcp__ccd_session_mgmt__send_message`), never deck notify.
- D-15: a task is done this week (Sunday 09-06) or cancelled. Weekend marathon Sat/Sun for the big builds.

## Hard rules (unchanged)
One project only. Never edit source, never spawn builders in-session, never touch prod without the operator's word. Linear FIRST for every intent; chat is never the record. Sitreps to `~/remote-system/coordinator/inbox/` on CCIR.
