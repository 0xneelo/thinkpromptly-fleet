# Brief for the ivy orchestrator — round-3 rulings (2026-09-04)
From 🧭 COORDINATOR 6 (lowcap coordinator portal). Repo: `~/projects/simple-ivy-vaults`. Team: SYN.
The operator answered the Backlog Unblock Round 3 sheet; these eight cards are yours. Every one carries the operator's note "should be handled by the ivy coordinator/orchestrator" unless stated.

## Rulings (choice → effect)
- **SYN-474 ledger** → **Ratify A**: deposits revert while a haircut is active. Implemented behaviour becomes ruled behaviour; close the issue.
- **SYN-495 dark-mode hero** → **Honour the export**: the dark hero on Overview/Directory ships as designed.
- **SYN-498 "could not be read" colour** → **Unify**: the worker picks the muted variant and sweeps all 6 call sites.
- **SYN-507 QA wallet** → **Retire the QA remainder**: SYN-504's stuck round-trip scenario closes unproven; the program already shipped.
- **SYN-534 de-slopify** → **Lane A docs slice only** (STE/Zinsser pass on Lane A docs; the full pass can follow later).
- **SYN-535 RDP login on the german box** → **Do it next box sitting**. Operator keyboard action (RDP `/login`); you schedule it with the operator and confirm workers regain Linear MCP + subagents.
- **SYN-538 Directory "best" column** → **Best openable premium** (never advertise an offer a user cannot take). No note.
- **SYN-539 P5 remainder** → **Fixture coverage** (deterministic fixtures, do not wait for a live round). No note.

## First actions (in this order)
1. Boot as this project's local orchestrator: `/local-orchestrator`, then end your first reply with the `/rename 🎛 ORCHESTRATOR <N> · simple-ivy-vaults · round-3 rulings` line.
2. Register EVERY ruling above as a Linear comment on its issue, titled `Operator ruling 2026-09-04 (unblock sheet, round 3)`, choice + note verbatim. Linear is the record; chat is not.
3. Apply the state the ruling implies (close / cancel / park) on the same pass.
4. For build items: package with `/introduce-goal` and launch on the german-box via `/german-box-workers`. The box is shared with lowcap-connector (11 lanes live at 11:00Z on 2026-09-04) — check capacity before minting.
5. Doctrine D-15: a task is done this week (by Sunday 2026-09-07) or cancelled. Priorities are not a queue.
6. Report completion to the coordinator portal `🧭 COORDINATOR 6` with `SendMessage` (or `mcp__ccd_session_mgmt__send_message`). Do NOT use deck notify for desktop seats — it reports delivered without rendering (DECK-23).

## Hard rules
- One project only: this repo. Forward anything else to its own orchestrator.
- Never edit source, never spawn builders, never touch prod without the operator's word.
