# Coordinator initiation — 2026-09-03 (provenance)

Board `coordinator/board.json` initiated 2026-09-03T21:33:43Z by the 🧭 COORDINATOR 4 portal
(`coordinator-init` ritual, remote mode from a lowcap-connector worktree). Operator present.
Seeded PROPOSED lanes L1–L4 and queue items OQ-1..3 (2026-08-28 snapshot) were replaced
(`--replace-proposed`; the operator had seen the list, 🧭 8, 2026-09-01).

## Northstar (operator, verbatim — ruling doc f0ed42edeea3, confirmed 2026-08-29)

> Pricing lowcaps accurately enough so that margin protocols can be built upon.

Follow-ups verbatim in `coordinator/northstar.md`.

## Operator words that authorised this initiation (verbatim)

- 2026-08-31 ballot (to Walter / 🔬 RESEARCHER 7, XYZ-1892): **"go with your ⭐ on all 22"** —
  net: new lanes C1(+C2), C3, C12 → N1, N2, N3; merge lanes 7+8 → L78.
- 2026-09-01 (🧭 8 portal, first-hand): **"merge 7+8, 1849 to inbox, seat picks"** and
  **"go ahead and initiate once the seat replies"**. "Seat picks" = the live seat names
  owners per lane and the concurrent staffing order; `lane_cap` 11 was drafted under that
  delegation (init_board.py needs cap ≥ open lanes) — **flagged for operator review**.
- 2026-09-01 first-hand: L4 (top-20 chains) = LANE, superseding the relayed defer.

## Owners + pick-6 — 🎛 ORCHESTRATOR 28 (O38), 2026-09-03 ~21:35Z, verbatim

> (a) OWNERS — L1 cyclomatic audit → O38 seat (to staff: refactoring-specialist; the complexity ratchet from train 76 is the instrument) · L2 one shared service for solana + evm → O38 seat (epic XYZ-1936; first slice = the node/WS cut step 1+2 pack on claude/node-ws-cut-step1-pack, launches after train 76.1) · L3 architecture swiss-cheese → O38 seat (gated on L1 + L2; staffed last) · L4 top-20 chains on one chain-agnostic layer → Konstantin (live, XYZ-2009 Chiliz on Ankr Premium; the new-chain checklist in docs/reports/chiliz-postmortem is the lane's spec; XYZ-2001 per-chain ws-mode/node-mode is its first milestone) · L5 ours_wrong diffs to zero → Lionel (live, XYZ-1924/1985/2012 riders; family-system review XYZ-2013 due 09-06 feeds it) · L6 consensus sweep verdicts reliable → O38 seat (XYZ-2010 tie-break/staleness gate + XYZ-2013 review first; storage XYZ-1893 is done) · L78 pool discovery + election + price method → Odilo (live, XYZ-1922 GT fallback delivered; XYZ-2002 UniV4 research + XYZ-1998 arming are its next inputs) · L9 diff auto-clear loop in production → O38 seat (D1 lever + Researcher 2's post-76 residual map are the inputs; XYZ-2011 dashboard tally rider first) · N1 push-route/train integrity + stranded-branch salvage → Jutta (live, git-workflow-manager; train 76 weaver; 76.1 next) · N2 CU/CG/RPC budget + metering truth → Claus (live, XYZ-1567 reserved shares + XYZ-2008 yield-bounded fallback; the 100 CU/s ingest floor re-tune is his open item) · N3 coordinator loop (boot gate, weekly reconcile, dead-man, operator-debt inbox) → O38 seat.
> (b) PICK-6, concurrent, in order: N2 (Claus) · L5 (Lionel) · N1 (Jutta) · L78 (Odilo) · L2 (seat, step 1+2 lane) · L4 (Konstantin). Then L9, L6, L1, L3, N3.
> No lane refused, none renamed. Recorded verbatim on XYZ-1827. Initiate.

Seat-owned lanes: the seat names the worker at launch and the board is re-pointed by a sitrep then.

## Milestone provenance — what is the operator's and what was drafted

Goals are the operator's words verbatim (lane cards 2026-08-31/09-01). `done_milestone` had
to be capability-shaped (two markers); the portion beyond the operator's words was drafted
under the "seat picks" delegation and is listed here so nobody mistakes it for a ruling:

| lane | drafted portion of `done_milestone` |
|---|---|
| L1 | second sentence |
| L2 | second sentence |
| L3 | everything after the dash |
| L4 | last sentence |
| L5 | everything after the dash (conviction mechanism per `tools/consensus/engine.py`; no single % exists) |
| L6 | entire milestone (ballot split: verdicts stay, storage → XYZ-1893) |
| L78 | entire milestone (ballot re-scopes b+e) |
| L9 | entire milestone |
| N1 | entire milestone (ballot C1+C2) |
| N2 | entire milestone (ballot C3) |
| N3 | entire milestone (ballot C12) |

## Inbox items at initiation (no lane; carried here until N3's operator-debt inbox exists)

- XYZ-1849 Phoenix/Rasa rebuttal — operator first-hand: **"ship once"**, reopen only if they publish again.
- Ballot inbox rows (docs/research/coordinator-lane-sweep-gaps.md @ 1bb00e7d, lowcap-connector):
  C5 provider ToS/redistribution compliance · C11 deploy/config truth · C13 fleet control-plane +
  seat lifecycle · **C14 prod secret exposure paths (urgent-tagged)** · C16 serving-path parity
  (LiveQuoteSource ×2) · C17 asset identity & quarantine.

## Pre-initiation sitreps archived, never applied

- `inbox/archive/2026-08-29T17:58:00Z-portal2-L5.md` — old L5 (RSC cure, XYZ-1849); its ruling is
  D-6 in `decisions-effective.md`; its subject is now the L5/L78 lanes + the XYZ-1849 inbox item.
- `inbox/archive/2026-08-29T20:53:00Z-c1-L3.md` — old L3 (fleet seat lifecycle) done-claim;
  fleet-side, recorded on XYZ-1872..1882 and the handoff index; nearest new lane N3.

## Placement (still an open operator ruling)

This board lives in the vendor repo (`coordinator/README.md`: "you are building the coordinator,
you arent using it", 2026-08-30) because the deck serves only this path (XYZ-1898, Backlog,
0 comments). Assumption applied: initiate here, move to lowcap-connector when XYZ-1898 lands
(a file move, not a re-init). Operator word "lowcap-local" overrides.

## Trail

Draft commits (D-7 mid-transit): 51d3943 · ec2eb0c · e2badc4 · 88d3a74 · d81f59b. Initiation = this commit.
