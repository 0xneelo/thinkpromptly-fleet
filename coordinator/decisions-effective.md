# Effective decisions

Currently-operative operator rulings (DESIGN §4.4). Superseded rows drop into git history — the
anti-rot mechanism. **Hard rule (DESIGN §2, failure #1):** auth / money / migration / delete /
scope rulings are **quoted verbatim or linked, never paraphrased** — the "one line" column is a
locator, not the ruling. Seeded 2026-08-28 by Sylvia: the *list* is PROPOSED (operator confirms
membership); the *quotes* are verbatim.

| id | one line | verbatim | source |
|---|---|---|---|
| D-1 | Boot-read gate is hard, not advisory | "**Boot-read gate: HARD.** PreToolUse-style hook denies orchestrator-seat work until the seat has read `board.json` and restated lane ownership. Advisory gates are proven failures (Class C/E)." | [DESIGN-v1.md §9.2](../docs/coordinator/DESIGN-v1.md) |
| D-2 | Overnight: reversible continues, irreversible holds + escalates | "**Overnight default: hold + Telegram escalation.** Reversible work continues; irreversible pauses hold AND escalate to a **Telegram bot with a simple accept button**. Operator checks the phone every ~4h including during sleep → ack-latency budget ≤4h. The Telegram accept-bot is a v0.2 build item (notify path first; button-ack second)." | [DESIGN-v1.md §9.3](../docs/coordinator/DESIGN-v1.md) |
| D-3 | The portal is a view, the files are the memory | "**Portal session approved.** The operator wants an interactive session to ask questions of, instead of clogging the orchestrator — \"the coordinator state should be in these files, yes; it's maybe just a portal.\" Adopted as §2a: state in files, portal as disposable view." | [DESIGN-v1.md §9.5](../docs/coordinator/DESIGN-v1.md) |
| D-4 | Each coordinator run is a disposable cheap session | "**Run owner: cheap session per run.** A timer/inbox-hook spawns a disposable cheap session for each coordinator run. True stateless reducer; the board is decoupled from seat health." | [DESIGN-v1.md §9.1](../docs/coordinator/DESIGN-v1.md) |
| D-5 | Coordinator v0.1 lives in the fleet repo | "(operator placement ruling: Coordinator v0.1 lives in the fleet repo, not lowcap — decided in-session 2026-08-28)." | [IMPORTED.md](../docs/coordinator/IMPORTED.md) |
| D-6 | Source coverage: want every source — no fixed caps, no minimum-N serve refusals; outlier detection scales with N | "i think we need more sources for anchor prices and outlier detections. our general policy should be if a token has 100 sources, we want 100 sources / if a token has 10 we want 10 / if a token has 5 we want 5 / if a token has 1 we want 1 / if a token has 1,000,000 sources, we want 1,000,000" (2026-08-29; retires ANCHOR_POOLS_PER_HOP=2; N=1 serves with degraded provenance; independence still counted — DS/GT/CG = one family) | [XYZ-1849](https://linear.app/synchronicity/issue/XYZ-1849), memory `source-coverage-policy-want-every-source` |
| D-7 | Coordinator context is stored mid-transit — in-flight init/interview state lives in `coordinator/` (committed), never only in chat or /tmp | "i mean your coordinator context...shouldnt that be in a single spot? … that seems to be a problem. the context should be already stored mid transit :))" (operator, 2026-08-29, portal c1 session; applied: interview draft = `coordinator/init-draft.json`, loudly non-binding, deleted by the initiation commit, never read by any run) | [init-draft.json](init-draft.json) |
| D-8 | Board `lane_cap` = 11 confirmed at initiation (closes the "drafted" flag) | "cap 11 ok" (operator, 2026-09-03T21:40:25Z, in the 🧭 COORDINATOR 4 portal chat, replying to the flag "lane_cap 11 was drafted, not your word") | [XYZ-1827](https://linear.app/synchronicity/issue/XYZ-1827) comment, [INIT-2026-09-03.md](../docs/coordinator/INIT-2026-09-03.md) |
| D-9 | WS-lane liveness evidence = `dex_events` rows + socket heartbeat; cursors/blocks are NOT WS-lane evidence | "cursor doesnt matter for our websocket lane" / "our websocket lane has no purpose of the cursors or blocks." (operator, 2026-09-03 15:08Z, chat; verbatim as recorded in memory `prod-reconcile-lane-off-cursors-frozen`; O38's XYZ-1990 recording paraphrases it) | [XYZ-1990 f5bdf075](https://linear.app/synchronicity/issue/XYZ-1990#comment-f5bdf075-cf99-43a0-bcfd-b14ea6ad1a5f), memory `prod-reconcile-lane-off-cursors-frozen` |
| D-10 | Node/WS split rulings D1–D10 (sheet 2026-09-03 ~17:05Z): delete the restart gap-walk fork; reconcile OFF on metered RPC, ON only behind an own node; V4 discovery = brain job + rename; rescue scan stays in the WS lane; WS owns `dex_events`, node owns headers+cursors; PerBlock/Hybrid behind feature `node-history`; wire fields kept, schema drops OFF; per-chain `ws-mode|node-mode` knob + restart + public chain-support page; pushed heads default; RFQ onchain arm deleted | LINKED — choices and the operator's notes verbatim in the comment, e.g. D1: "we should have a setting in admin dashboard and the ability to restart the service. then we can select ws-mode or node-mode for each chain seperately." | [XYZ-1936 38ccd1c2](https://linear.app/synchronicity/issue/XYZ-1936#comment-38ccd1c2-6f03-4699-abc5-e78024a87477) |
| D-11 | No head polls on the WS lane (refines D5/D9): ws-mode chain = no head poll, no cursor, no block headers; liveness = socket ping/pong + subscription lease | "why would we do any head tracking on the ws-lane?" → "Yes — no head polls on the WS lane." (2026-09-03 ~22:40Z) | [XYZ-1936 b5c1df97](https://linear.app/synchronicity/issue/XYZ-1936#comment-b5c1df97-1dd1-400b-b210-c5b0f4634cfe) |
| D-12 | Lane CU reserves stay file-configured, read-only on the admin route | "keep it file-only, that's fine" (2026-09-03 ~16:13Z, typed into Claus's pane, recorded by O38) | [XYZ-1567 e25e86e9](https://linear.app/synchronicity/issue/XYZ-1567#comment-e25e86e9-d2a6-4ca5-89e5-4c8c6b59d76e) |
| D-13 | Solana tie-break: `raydium` is the second breaker when the jupiter family abstained; never our own elected pool | "A, use raydium as the second breaker" (2026-09-03 ~20:40Z) | [XYZ-2012 6fe046c2](https://linear.app/synchronicity/issue/XYZ-2012#comment-6fe046c2-59e4-48e3-a336-6fc4113d21cf) |
| D-14 | Family-system review ordered, due 2026-09-06 (XYZ-2013; 17 verdict/tie-break issues folded under it 2026-09-04) | "make a note that the operator thinks the whole family system might have to be looked at and improved in next 3 days." (2026-09-03 ~20:40Z) | [XYZ-2012 6fe046c2](https://linear.app/synchronicity/issue/XYZ-2012#comment-6fe046c2-59e4-48e3-a336-6fc4113d21cf), [XYZ-2013](https://linear.app/synchronicity/issue/XYZ-2013) |
| D-15 | Backlog doctrine: a task is done this week or cancelled; priorities are not a queue | "priorities are irrelevant — we either do the task this week or we cancel it." (2026-09-03 night, KEEP resolution sheet) | [XYZ-1805 ff48f0ba](https://linear.app/synchronicity/issue/XYZ-1805#comment-ff48f0ba-76ec-49b7-b453-e4b1695e5bfe) |
| D-16 | CG credit governor: `compare_sampler_token_pools.daily_share` = 0.05, governor stays `enabled=false` | "Yes — PUT the 0.05 share now" (2026-09-03 ~20:55Z) | [XYZ-1998 cf85fc5e](https://linear.app/synchronicity/issue/XYZ-1998#comment-cf85fc5e-03f6-4366-bb70-1210aa41beb1) |
| D-17 | Backlog burn 2026-09-04: evidence sweep (257 issues) → KEEP sheet 16/16 ruled + breakfast bulk orders (close DONE, cancel superseded, move misfiled, packs superseded per pack); applied per issue as "Operator ruling 2026-09-04" comments | "Evidence sweep tonight, bulk actions in the morning." (2026-09-03 21:25Z); "Close all 16" (2026-09-04 01:40 local); KEEP sheet 16/16 "every card on the ⭐ line" (~02:05 local) | lowcap-connector `docs/research/backlog-burn-2026-09-04/` @ `e3807f63` (unpushed at recording, O38 pushes), [XYZ-1998 362fad78](https://linear.app/synchronicity/issue/XYZ-1998#comment-362fad78-18ac-42f9-82a5-402094831c7d) |
| D-18 | Chiliz (88888) runs on Ankr Premium: keyed WSS primary, publicnode fallback, keys live only in the VPS `.env` (never a full push of production.env) | "premium booked. lets use ankr premium i provide the keys into the .env now" / "its on production.env main" (operator, 2026-09-03 ~21:00Z, chat — as relayed by O38 in its 23:2xZ day report; not on the tracker verbatim, operator may confirm) | [XYZ-2009](https://linear.app/synchronicity/issue/XYZ-2009), sitrep `coordinator/inbox/archive/2026-09-03T23:25:00Z-o38-L4.md` |
| D-19 | WS lane fixed per the 2026-09-04 WS/RPC provider research: complete WS/RPC separation (refines D-9/D-10/D-11). Transport truth = RFC 6455 Ping/Pong only; subscription truth = explicit epochs/leases (never reuse a server ID across sockets); data truth = delivered frames or independently pushed unmetered reference only; budget denial is neutral, never provider failure; ALL metered liveness probes on the WS lane stay banned (incl. 0-CU `net_version`); `eth_getLogs` only as bounded post-reconnect repair. v2 audit deltas: instrument the EFFECTIVE alloy runtime path first (deployment sends no protocol pings despite the crate default); coverage decomposed into local_task_live / transport_live / lease_live / known_gap / budget_state / data_confidence, with `sparse_unknown` reported honestly, never as healthy; lease/socket rotation ONLY where a documented provider limit or measured decay justifies it, no blind renewal schedule; N expected blocks with no event never convicts a sparse filter; PublicNode is the sole keyless reference candidate (CHZ+RH yes, HYPER no), Blast deprecated, LlamaNodes gone | "ok we just learnt everything about websockets and how to properly handle rpc websocket connections. lets record this and contact the orchestrator to properly fix our ws-lane so we can finally once and for all completly seperate websocket and rpc" (operator, 2026-09-04 ~00:5xZ, 🧭 COORDINATOR 6 portal chat, handing over the research doc) | [docs/research/ws-rpc-provider-practices-2026-09-04.md](../docs/research/ws-rpc-provider-practices-2026-09-04.md) (v2, GPT 5.6-audited 2026-09-04, supersedes v1 @ 2a71f43; build HOLD lifted), lane L2 / [XYZ-1936](https://linear.app/synchronicity/issue/XYZ-1936) → [XYZ-2020](https://linear.app/synchronicity/issue/XYZ-2020) (truth split, extended by O39) + [XYZ-2021](https://linear.app/synchronicity/issue/XYZ-2021) (PublicNode canaries, gated on v2) |
| D-20 | Lane ownership: the live orchestrator seat may proxy-report (file sitreps) for worker-owned lanes; box workers stay the registered owners. Closes the run-1 (2026-09-03T23:31Z) wrong-owner exception that archived the seat's L4/L5/L78/N1/N2 sitreps; the run's owner check must admit seat-authored sitreps on those lanes | "a for the ownership ruling" (operator, 2026-09-04 ~09:35Z, 🧭 COORDINATOR 6 portal chat — option (a) of the portal's three: "(a) seat may proxy-report for worker-owned lanes, (b) transfer ownership to the seat with workers as executors, (c) workers must file their own sitreps") | [XYZ-1827](https://linear.app/synchronicity/issue/XYZ-1827), run-1 flags `coordinator/inbox/archive/2026-09-03T23:2*Z-o38-{L4,L5,L78,N1,N2}.md.flag` |
| D-21 | Mint wave 1 — operator word given: staff the unstaffed lanes L6 (XYZ-2010 + XYZ-2013 first) and L9 (XYZ-2011 rider first) plus the ruled-but-never-minted + KEEP lanes listed under N3; clears the `next_decision: operator word: mint wave 1` on L6/L9/N3. Precondition still operator-only: re-mint the deploy cert (expired 2026-09-04T03:37Z per the seat) before any box launch | "mit wave 1" (operator, 2026-09-04 ~10:05Z, 🧭 COORDINATOR 6 portal chat, typed mid-turn while the unblock sheet was being scoped — read as "mint wave 1", the exact phrase the board asks for) | board `next_decision` on L6/L9/N3 (2026-09-03T23:26Z), [XYZ-1827](https://linear.app/synchronicity/issue/XYZ-1827) |
| D-22 | cg_wrong rows (80 on the 2026-09-04 board) are NOT display-only comparator facts: every cg_wrong conviction must be verified — prove we are right before the row is attributed to CoinGecko. Verification route to be ruled on the unblock sheet (hunt first vs worker lane) | "we need to verify really really really well that we are actually right about these cg_wrongs ." (operator, 2026-09-04 ~10:10Z, 🧭 COORDINATOR 6 portal chat, mid-turn, answering the cg_wrong-80 scope question) | board verdict counts pasted by the operator 2026-09-04 ~09:55Z; lane L5 milestone (zero ours_wrong) does not cover cg_wrong |

## Archive

None yet. A superseded ruling moves here with `supersedes:` / `superseded-by:` ids, then drops out
of the loaded bundle at the next weekly pass.
